# PostHog API Key Reference

| Field       | Value                                                        |
| ----------- | ------------------------------------------------------------ |
| **API Key** | `phx_REDACTED`       |
| **Host**    | `https://us.i.posthog.com`                                   |
| **Project** | VaPls Discord Bot — Observability & Analytics                |
| **Type**    | Personal API key (phx\_) — used for data export / API access |
| **Env Var** | `POSTHOG_API_KEY` in `.env` and `userbot/.env`               |

> **Security**: This key has full project access. Keep it out of git
> (it is already in `.gitignore` via `.env` patterns).
