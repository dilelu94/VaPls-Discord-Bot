[pytest]
# asyncio_mode = auto lets us write `async def test_*` without per-test markers.
asyncio_mode = auto
testpaths = tests
# Collect both the new snake_case files and the legacy camelCase file.
python_files = test_*.py testSoundpad.py
filterwarnings =
    ignore::DeprecationWarning
